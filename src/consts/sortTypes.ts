export enum sortOrder {
    asc = "asc",
    desc = "desc"
}

enum sortTypes {
    release_date = "release_date",
    runtime = "runtime",
    vote_average = "vote_average"
}

export default sortTypes;
